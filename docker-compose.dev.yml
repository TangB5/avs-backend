# Surcharge pour le développement local (volumes montés, hot-reload)
version: '3.9'
services:
  api:
    build:
      target: builder
    command: npm run dev
    volumes:
      - ./src:/app/src
      - ./prisma:/app/prisma
    environment:
      NODE_ENV: development
    profiles:
      - dev
